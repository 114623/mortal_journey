# 更新日志

## 2026-09-25

> 验证：`vue-tsc --noEmit` + `vite build` 通过。

### 健壮性与体验改造（robustness-spec §A~§G）
- **AI 超时可调 + 传输层退避重试**：新增 `aiTimeoutStore`（60~900s，默认 420s）；
  `openAiChatBridge` 修复 `Promise.race` 僵尸请求/计时器泄漏，429/5xx/超时/断网自动重试
  最多 2 次（退避 2s·2ⁿ + 抖动），可被用户取消；设置弹窗新增「AI 请求超时」滑杆。
- **状态 AI 输出契约重排**：16 段 → 17 段，小标签前置、大段分散、战斗触发与选项固定收尾；
  各规则段标注契约段号；缺失标签时剧情栏插灰色 notice 气泡兜底。
- **NPC 快照配额**：休眠段只列最近 8 名、羁绊段留 6 名，超出折叠；快照超 4000 字告警。
- **防复读**：状态 AI 注入最近 3 轮选项（最多 12 条）；`optionDedup` 按 ≥8 字公共子串
  程序化去重，保底不足 2 条时回补。
- **收束锁改良**：非战斗回合按 0.5 计入场景配额；强制清零时程序代写转场旁白并推进 1 天。
- **开局初始状态失败可见化**：失败不再吞掉，弹 warning 条 + 「重新生成初始状态」按钮。
- **主线进度反馈闭环**：状态 AI 新增 `<MJ_MAINLINE_TAG>` 每回合必报；回注剧情 AI 最近 5 条；
  世界设定弹窗「主线·篇章」页新增只读进度列表。

### 输出截断检测与重试
- 状态 AI `max_tokens` 16384 → 65535（与剧情 AI 对齐）。
- `openAiChatBridge` 检测 `finish_reason === "length"` 抛 `AiOutputTruncatedError`，
  不再把残缺正文当成功。
- `StoryChatPanel.callStateAI` 失败自动重试一次，截断重试带「大幅精简」要求；
  界面红字说明「什么没落地 + 能怎么办」。

### 功法体系重构 v4：数值锚定层号
**核心：任何功法第 L 层的主属性加成完全相同，与阶层、使用者境界无关；
阶层只决定能修到第几层。**
- 层数上限：凡人 3 / 练气 7 / 筑基 10 / 结丹 13 / 元婴 16 / 化神 19。
- 全阶层统一经验曲线，同层同价；继承机制改「复制 + 夹取」。
- 主属性 `gongfaLayerValue`：层号 → 加成值（极品锚点，几何插值），品阶按
  下品 33% / 中品 59% / 上品 81% / 极品 100% / 仙品 115% / 神品 130% 缩放。
- 连续层号：层内成长立刻生效，不再等整数层跳变。
- 战斗技能保留使用者口径（`scalingRatio × 实时属性`）；战斗倍率按连续层号
  （练气满层 1.45 → 化神满层 2.35）。
- 法力消耗改占自身最大法力百分比（下品 15%~20% → 神品 24%~36%）。
- `gongfaTierFactor` 改为仅凡人生效（练气期 40%、筑基起 0）；法宝/丹药压制不动。
- 删除 `GONGFA_ATTRI_CAP_BY_TIER` / `gongfaAttriMult` / `resolveGongfaLayer10` 等旧常量。

### 功法技能绝对点数改锚层号
- 技能 `baseValue` 等绝对点数改由 `gongfaBvValue`（层号 → 点数）给出，同层同值，
  不再乘 `gongfaSkillRealmScale`（已 deprecated，无调用方）。
- 锚点：L1 = 练气后期血 7%，其余各阶层满层 = 后期血 11.4%。
- 类型系数 `BV_TYPE_MULT`（伤害 1.0 / heal 0.9 / shield 1.2 / counter 0.7 / summon 0.45 / tick 0.35）。
- 体系系数 `SYSTEM_BV_MULT`（魔修 1.7 / 法修·毒修 0.7 / 其余 1.0）。
- 实测：练气中期极品 L2 从 19 点 → 53 点；练气功法练满带到化神 = 0.68× 普攻（自然淘汰）。

## 2026-09-24

### 境界表重排：每个大境界 ≥ 2.5 倍
- 调整结丹后期 / 元婴全段 / 化神全段共 7 行；凡人~结丹中期不变。
- 元婴气血下调、攻防上调（差距搬回攻防，不再靠堆血）。
- 身法/悟性例外（1.57~2.0×），避免高阶一回合连出十几次手。
- 复核 15 组全部达标；属性实时派生，老存档立即生效。

### 器灵封印放宽：TIER_SUPP_HIGH 0.35 → 0.70
- 越一阶实际数值 = 同阶 105%~140%，达到「稍强于同阶」。
- 已知副作用：越阶越多越强（越五阶可达 202%），留待后续给 Δ≥2 设陡曲线。

### 绝对加成的境界缩放（`realmScale.ts`）
- 功法词条、丹药定值效果乘系数使占比在各境界恒定；启用起点「结丹」。
- 功法目标占比 0.5，丹药 0.06（消耗品可囤，刻意低一个量级）。
- 浮点累加后统一取整 + 保底 1，避免小幅加成被截成 0。

### 物品面板显示「实装有效值」
- 法宝/功法/丹药三类都标出乘过跨阶压制与境界缩放后的实装值；
  修复功法加成此前完全没乘跨阶压制的 bug。

### 战败结算：主角倒地不再立即判负
- 去掉 `protagonistDead` 短路，胜负只看双方是否还有活人；
  主角倒地播「重伤倒地」日志，队友可继续打。
- 死斗胜方存活率实测 69.97%（4 万次），败方 100% 真死，切磋不死。

### 持久状态（buff）系统首版
- 新模块 `characterBuff.ts`，时间口径 = 世界时间天数（不用回合数）。
- 首版只做血/法上限：首条模板「气血亏虚」（血上限 −30%、30 天、同名不叠加只续期），
  死斗主角倒地存活时挂上。
- 状态 AI / 剧情 AI 注入当前持久状态行，避免 AI 误判主角强度。

### 修「主角记忆被 JSON 外壳污染且每轮自我复制」
- `memoryLog.stripMemoryJsonShell` 在写侧 / 提示词侧 / 解析侧各堵一道。
- `TIME_LINE` 正则补「同日时段 / 跨月跨日区间」。
- 实测：4922 字 → 978 字，14 条日志无重复。

## 2026-09-23

### 凡人阶物品：练气期 10% → 40%
- `itemTier.ts` 的 `MORTAL_TIER_SUPP[1]` 上调；凡人期同阶 100%、筑基及以上仍 0。

### 新建角色卡（手动造人）
- 侧栏「角色」总览顶部「＋ 新建角色卡」，复用角色设定表单，填好才入库。
- `Npc.createManualNpc()`：境界默认练气初期，画像默认 `source:"manual"`（AI 不改写）。
- 校验：名字必填、不得与已有角色同名。

### 战斗触发后可自由编排参战人员
- 新增 `BattleRosterModal.vue`：开战前逐人三选一（我方 / 敌方 / 不参战），
  默认按状态 AI 名单预填；主角恒在我方，每方上限 5，敌方至少 1 人。

### 侧栏高度策略：整块滚动
- 废弃 flex 分区（矮屏卡片区被压到看不见），改为面板整体 `overflow:auto` 滚动。

### 无灵根硬锁：不得突破出凡人
- `realmUtils` 统一判定入口；主角/NPC/AI/UI 全部读同一份。
- 修为可攒满，境界不许动；凡人内部精进不受影响。
- 修 `LINGGEN_CULTIVATION_MULT` 无灵根兜底（补 0: 0.2，比五灵根还慢）。

### 主角可编辑基本信息
- 主角「角色设定」弹窗新增基本信息区（名字/性别/境界/年龄/寿元/灵根），
  与 NPC 共用待应用队列。
- 主角改境界：主属性重设、修为归零、气血回满、功法层数不动。
- `NpcBasicsDraft` 更名 `CharacterBasicsDraft`，主角 NPC 共用读写。

## 2026-09-21

### 主角记忆系统（AI 维护）
- 状态 AI 新增 `<MJ_PROTAGONIST_MEMORY_TAG>`，格式与 NPC 记忆一致（三行日志）。
- 写入通道 `Character.appendAiMemoryEntries()`，只动 memory，不受画像锁定影响。

### 修「不生成推进选项 / 有战斗却不触发」
- 记忆日志示例裸换行导致 JSON 解析失败 → 改 `\n` 转义 + 解析层容错。
- 编号撞车重排；输出膨胀时省略 memory 字段；兜底重试只取选项与战斗触发。

### 取消「功法已不入流则修炼不产修为」
- 功法阶层只影响跨阶压制，不再卡修为。

### 修「剧情五个人只建一张卡」
- `npcStore` 同回合同名条目视为另一个人，自动改名建卡；提示词禁止同名条目。

### 记忆守卫加固
- 无时间行的玩家散文靠 Dice 相似度认亲，AI 润色版被丢弃；
- 删条目单独检测并回滚；主角记忆 `full` 只在压缩轮接受。

## 2026-09-20

### 场景配额改版：程序侧回合预算 + 收束锁
- 新增「每层/每轮回合数」配额（1~5，默认 2）；层/轮上限 × 此值 = 场景总回合预算。
- 收束锁触顶后每回合持续注入收束令并逐级加压，超 3 回合强制清零。
- 硬闸：收束锁期间程序直接丢弃 `<BATTLE_TRIGGER_TAG>`。

### 推进选项协议 v2
- 元信息四字段：`（类型 | 距离 | 主动方 | 推进轴）`，「距离」分贴身/邻近/旁支/远离。
- 旧元素搁置：3 回合前未选的悬念元素不得再进选项或正文。
- 上轮选项回传，本轮至多 1 条与上轮核心对象相同。

### 势力系统
- 新增势力档案（驻地/战力/诉求/关系），强度档位由程序按战力推导。
- 来源：侧边栏「探查」生成 3~6 家 + 状态 AI `<MJ_FACTION_TAG>` 增量上报。

### 篇章系统
- 「剧情脉络」升级为主线 + 篇章；不开启不注入。
- 超过回合上限后逐级温和加压，不设硬闸。

### NPC 记忆改为三行日志（只追加）
- 时间行 / 地点行 / 正文行，新的在最上面；`guardMemoryUpdate` 守卫只追加。

## 2026-09-19（晚）

### 战斗：身法闪避
- 身法影响命中率；闪避 `= 35% ×(守方身法 − 攻方身法)/(两者之和)`，上限 50%。
- 被冰冻/眩晕时闪避归零。

### 推进选项改用「推进选项生成协议」
- 4 条并列分支，每条带元信息；推进轴跨回合轮换。

### 文生图：古典油画肖像
- `promptBuilder.ts` 重写为浪漫古典仙侠油画模板。

### 场景配额（初版）
- 秘境/擂台/同场景战斗波次上限可在游戏内修改；状态 AI 用 `<MJ_SCENE_TAG>` 报告进度。

## 2026-09-18

### 人物系统
- 主角与 NPC 新增「性格/外貌/记忆」画像，AI 逐回合维护，玩家保存即锁定。
- 人物档案总览按在场/休眠/离开/已故分组；回合中修改进待应用队列。
- NPC 卡可删除、可编辑基本信息；记忆超 1000 字由 AI 压缩。

### 物品与数值
- 阶层系统覆盖全品类：法宝跨阶压制 0.65^Δ；低阶丹药对高一阶效果 1/5。
- 功法阶层兜底（`ensureGongfaTier`）；功法层数上限按阶层解耦。
- 品阶越高修炼越慢（下品 1.25 → 神品 0.7）。

### 战斗
- 死斗/切磋定性；战后处置建议（补刀/索取/逼问/饶命）；战后战斗描写由 AI 撰写。

### 界面
- 天道编辑（物品/功法/储物袋改品级阶层词条，支持角色间转移）。
- 剧情脉络、世界设定编辑、游戏内读取人生、回合自动存档、设置弹窗（字号/配额）。

### AI 与提示词
- 预设拆分；合并「双星纪 V1.1」；NPC 一致性校验零 token。

### 工程化
- git 初始化；新增 DEPLOY.md。
