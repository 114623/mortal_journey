# 更新日志

## 2026-09-18

> 验证：`vue-tsc --noEmit` + `vite build` 通过（167 modules）。

### 人物系统
- 主角与 NPC 新增「性格 / 外貌 / 记忆」画像，AI 逐回合维护；玩家保存即锁定不被覆写。
- 新增人物档案总览：全部角色列表，按在场/休眠/离开/已故分组。
- 回合中修改画像/世界设定进「待应用队列」，回合结束后统一落地，随存档持久化。
- NPC 卡可删除（二次确认），可编辑 名字/性别/年龄/寿元/境界/灵根；改境界自动重算属性、血量与功法层数。
- 记忆超 1000 字自动由 AI 压缩至约 700 字。

### 物品与数值
- 阶层系统覆盖全品类：法宝跨阶压制 0.65^Δ；低阶丹药对高一阶修士效果 1/5；材料异阶同炉拒绝；功法境界高于阶层后不再产修为。
- 法宝跨阶压制恢复双向：~~法宝低阶不削弱~~（09-19 已取消该规则）——低阶法宝同样按
  0.65^Δ 削弱，仅保留器灵封印（高阶）与凡人阶专属衰减；老存档功法阶层迁移已自动补「与持有者同阶」。
- 品阶约束：NPC 按战力档限品阶区间，主角不限制；AI 显式 grade 优先。
- **功法阶层兜底**：AI / 命运抉择 / 天道编辑漏填 `tier` 的功法，在进入角色（入袋 / 装备）时由
  `ensureGongfaTier` 按持有者当前境界**就地固化**阶层（补写瞬间数值不变，此后突破即正常衰减、
  境界高出后计入「已不入流」）。整体赋值的两条路径（`Protagonist.applyInitState`、
  `Npc.applyCoreChange` 重建功法栏）另用 `ensureGongfaTierList` 二次兜底。老存档仍走读档迁移
  `backfillGongfaTiers`，三者共用同一规则。修掉了「无阶层功法永久免疫跨阶压制」的漏子。
- **功法层数上限按阶层解耦**：层数不再一律 10，改为凡人 3 / 练气 5 / 筑基 6 / 结丹 7 / 元婴 8 / 化神 10。
  倍率由「写死查表」改为**按修炼进度比例插值**：主属性上限按阶层封顶（凡人 2.5× ~ 化神 10×），
  战斗倍率上限恒为 2.35×。阈值按层数上限在基准累计曲线上等比切分（10 层档逐项等于旧表），
  层号经 `resolveGongfaLayer10` 映射到 10 层数值曲线的连续位置——低阶功法层数少但每层跨度大。
- **续作承继修炼进度**：得到某门功法的后续篇（筑基篇 / 前辈续写 / 补全残卷）时，可沿用原功法进度。
  AI 侧新增 `inheritFrom` 字段（状态 AI 提示词 11.4），程序按**进度比例**折算到新功法层数并保留最后
  一层不继承（source 满层 → 续篇到「上限-1」层）；天道编辑的功法页也新增「承继修炼进度」下拉可手动指定。
- **品阶越高修炼越慢**：`GONGFA_GRADE_CULTIVATION_MULT` 由 下品1.0→神品1.25 翻转为
  下品1.25 → 神品0.7（神品约比下品慢 1.8 倍），并修正此前写反的表头注释。
- 「熟练度 / 熟练等级」文案统一改为「**修炼进度**」；功法详情新增「阶层」条目，修为门槛并入其中显示。
- 境界表化神段重平衡：按元婴后期基数 ×1.25/1.5/1.75 重排，消除顶部趋平。
- 老存档迁移：层数上限收窄后，超过新上限的旧功法读档时自动夹回满层（`clampGongfaMasteryInHolder`）。

### 战斗
- 法宝/功法/丹药阶层压制接入战斗引擎，含战斗内服药链路。
- **死斗/切磋**：开战时按剧情定性（死斗 / 切磋），战斗界面顶栏显示标识。切磋血量归零不死；
  死斗中**只有胜方**倒下者享七成存活（重伤），败方倒下者无优待。
- **战后处置**：战斗结束不再直接判定敌人死亡——先生成四个处置建议（补刀 / 索取财物 / 制服逼问 / 饶命放走），
  玩家点选或自己写一句，AI 据此描写收场，死亡由状态 AI 依剧情落地；选项生成失败则直接输出。
- **战后战斗描写**：战后不再直接写"胜/败"结论，改由 AI 依据战况纪要撰写交手过程。

### 界面
- 天道编辑（原物品锻造）：主角与 NPC 的法宝/功法/储物袋均可改品级、阶层、词条、效果，支持角色间转移，改动即时生效落盘。
- 新增「剧情脉络」：写下希望的剧情发展方向供 AI 参考（世界设定第四个标签页，侧边栏有独立入口）；
  非空时注入剧情 / 开局 / 修炼 / 结局四条叙事链路，空则不注入（省 token）。
- 世界设定界面：世界观/规则/预设三段编辑，支持恢复默认。
- 游戏内读取人生：读取/导出/删除，切换前自动落盘。
- 回合自动存档：每回合快照，滚动保留 0–10 份（默认 3），配额不足自动丢最旧。
- 侧边栏新增各入口（带待应用角标）。
- 新增「设置」：可调节剧情正文字号（0.8×–2.0×，快捷档位 + 实时预览），存 localStorage，刷新后保留。

### AI 与提示词
- 预设拆分：输出契约固定，世界观/规则/状态模板可编辑；剧情与状态 AI 共用世界观。
- 合并外部预设「双星纪 V1.1」（文风、对白、反全知、叙事约束等）；NSFW 系列未合并，已导出为 txt。
- NPC 一致性校验：正文境界与 NPC 卡冲突时就地纠正，零额外 token。
- 每回合新登场有名 NPC 限 1~10 人；世界设定注入全部五个生成链路。
- **内置默认设定替换**（2026-09-19）：以存档「于金时」（09-19 15:36）中的三段为新的内置默认——
  世界观新增第 13 条「天南世俗王朝与朔国周边列国格局」+ 非同门称呼三档；
  规则改剧情长度 500-800 字、修炼年数大幅上调（练气 5~20 / 化神 400~600 年）；
  文风预设由 84 行扩充到 397 行（含玩家追加条目与 NSFW 段）。旧版备份在 `.workbuddy/backup/`。

### 存档与数据
- 世界设定双副本：全局模板 + 存档副本，读档不污染全局。
- 自动存档快照随主存档级联删除；待应用队列先改 NPC 基本信息再改画像（改名重建键）。

### 修复
- 主界面占位文案按阶段显示真实状态；画像字段截断导致记忆永不压缩的问题已修复。
- AI 显式输出的物品品阶不再被丢弃；老存档功法读档时自动补「与持有者同阶」（不再永久免疫压制）。

### 工程化
- git 初始化（146 文件，.gitignore 生效）；新增 DEPLOY.md 部署教程。

### 待办
- 功法层数上限：诊断完成未实施，方向为层数上限与满层倍率解耦、倍率等比化。
- NSFW 系列（27 条）在提取 txt 中，可自行粘贴进世界设定。
- localStorage 约 5MB 上限，立绘过大时调小自动存档保留数。
- git commit 需先配置 user.name / user.email。

## 2026-09-19（晚）

> 验证：`vue-tsc --noEmit` + `vite build` 通过（175 modules）。

### 战斗：身法闪避
- 身法不再只影响出手频率，也影响难不难被打中。有效身法 `= speed ×(1 + speed 修正%)`，
  与行动条同一口径（`formulas.effectiveSpeed`，`GaugeManager` 复用）。
- 身法闪避 `= 35% ×(守方身法 − 攻方身法)/(两者之和)`，守方不占优时为 0（不倒扣命中）。
  比值型天然递减：身法高一倍约 11.7%，跨大境界约 11.7%~30.6%，永远逼近不到 35%。
- 最终闪避 `= min(50%, dodgeRate 修正 + 身法闪避)`；被冰冻 / 眩晕时归零（`EffectManager.isImmobilized`）。
- 战斗 trace 增补身法差贡献明细，便于核对数值。

### 推进选项：改用「推进选项生成协议」
- 每回合的快捷选项不再是四个固定倾向（激进/中庸/谨慎/最谨慎），改为 **4 条并列分支**，
  每条带元信息：`（类型 | 承接 | 推进轴 | 主动方 | 强度）[正文]`。
- 并列分支契约：所有选项都从同一正文尾部出发、互斥、互不继承其它选项新增的事实；
  第 1 项标「承接:钩子」，另有至少 1 项标「承接:另起」。
- 推进轴跨回合轮换：本轮至少覆盖两类核心轴（浪漫/艳遇、危机/压力、机缘/异常），
  最近 3 轮用过的轴会回传给状态 AI 让它补齐（`storyStore.branchAxisRounds`，随存档持久化）。
- B1 去人称硬规则：主动方为主角时正文用省略主语的动作叙述。
- 解析器 `parseActionOptions` 兼容协议文本 / JSON 数组 / 旧四倾向对象三种写法；
  `normalizeActionSuggestions` 在读档时统一归一化，旧存档不会渲染出空按钮。
- 开局预设与战后处置预设同步改为协议格式；界面在每条选项上显示推进轴小标签。

### 文生图：古典油画肖像
- `promptBuilder.ts` 重写为「浪漫古典仙侠油画 · 古典大师画作」模板：
  subject_type / gender / action / location / portrait_prompt / figure / appearance /
  appearance_details / hair_style / attire 等变量由角色字段映射，NPC、主角、地点三套共用同一画风。

### 场景配额：防无限刷波
- 新增 `sceneBudgetStore`：三个**可在游戏内修改**的配额 —— 秘境层数上限（1~9，默认 3）、
  擂台轮次上限（1~9，默认 3）、同场景连续战斗波次上限（1~6，默认 2）。
  「设置」弹窗新增一组滑杆，配额存 localStorage（跨存档生效）。
- 状态 AI 每回合用 `<MJ_SCENE_TAG>` 报告场景进度（kind 秘境/擂台/无、name、stage、total、ended）；
  total 一律夹到配额上限，AI 不得自己把秘境写成十层。
- 程序侧兜底：即使 AI 不报标签，也按「同一地点内已连打几场」独立计数，换地点自动重新计。
- 触顶时向剧情 AI 与状态 AI 注入【场景配额·硬约束】，强制本回合收束场景；
  触顶前一层先给一次软提醒，让 AI 有节奏地把场景收掉。
- 场景进度只在会话内维护（开新人生 / 读档清零），避免旧进度与新存档打架。

## 2026-09-20

> 验证：`vue-tsc --noEmit` + `vite build` 通过（175 modules）。

### 场景配额改版：从「AI 自报层数」改为「程序侧回合预算 + 收束锁」
- **主轴换成程序侧的回合预算**：新增配额「每层 / 每轮回合数」（1~5，默认 2）。
  层/轮上限 × 此值 = 场景总回合预算（3 层秘境 = 6 回合）。层/轮的推进由 `noteSceneTurn()`
  按回合数自动推进，AI 报的 stage 只用于「往前跳」——AI 赖在第 1 层不报进度也没用。
- **收束锁（closing）**：触顶后不再只给一次指令，而是每回合持续注入收束令并逐级加压
  （第 1 回合给「脱离 + 结算 + 转场」正向清单；第 2 回合起要求一句话转场、禁止任何新内容）；
  超过 3 回合仍未收束则程序强制清零，避免死锁。
- **硬闸**：收束锁期间程序**直接丢弃** `<BATTLE_TRIGGER_TAG>`——不是求 AI 别打，是让它打不起来。
  这是整套配额里唯一一道 AI 无法绕过的拦截。
- **指令改写**：由「禁令堆砌」改为「正向清单 + 禁令」；未触顶时只给一句轻量进度提示，不干扰叙事。
- 同场景战斗上限默认 2 → 3（一场战斗通常占 2 回合，与 3 层 × 2 回合的预算匹配），上限放宽到 9。
- 设置弹窗新增「每层 / 每轮回合数」滑杆；配额存储键升级为 `MJ_SCENE_BUDGET_V2`。

### 界面
- **推进选项改为追加**：点击选项不再清空输入框已有的字，改为换行追加（同一条不重复追加），追加后自动聚焦。
- **角色（原「人物档案」）**：侧栏入口与弹窗标题改名为「角色」；角色卡换成世界地图同款卡片
  （84px 头像 + 姓名 + 境界 + 身份 + HP/MP 条，新增共用组件 `NpcMiniCard.vue`）；
  点 NPC 卡打开与世界地图一致的信息界面（`NpcDetailModal`），主角卡直接进角色设定编辑。
- **「档案」→「角色设定」**：NPC 信息界面的按钮与画像编辑弹窗标题统一改为「角色设定」。
- **侧栏在场人物**：角色按钮下方直接列出当前地点的在场人物卡（同款样式，头像压缩到 46px 适配窄栏），
  按最近出场排序，点开即信息界面。
### 界面（后续微调）
- **角色卡右侧「📝」**：打开「角色」后，每张卡右侧空白处直接有角色设定按钮，一步进编辑，不必先点开信息界面。
- **侧栏在场人物**：移到功能面板最顶部；去掉「在场人物」标题与计数、去掉外层框；
  头像 46px → 72px（卡片整体放大），代价是血条缩短压矮（限宽 92px、高 6px、隐藏 HP/MP 文字标签，靠红/蓝辨色）。
- **折叠开关改为正方形按钮**：移除原来的 26px 竖条；展开态——左栏在日期条右侧空白处（absolute 定位，
  日期文本仍居中），右栏在场人物区左上角；折叠态——栏内顶部居中一颗 24px 按钮用于展开。
  待应用角标改为挂在按钮角上（不挤占箭头），展开态与折叠态都能看到。
- **天道编辑角色顺序**：改为纯按最近出场排序（已故者沉底、组内仍按最近出场），与「角色」列表一致。
- **角色卡头像贴边 + 放大**：卡片去 padding/gap + `overflow:hidden`，头像 `align-self:stretch`、去掉自身圆角，
  只留一道竖向分隔线，圆角交给卡片裁切，头像顶到卡片左/上/下三边。
  头像 84 → **104px**（信息区改用自身 `padding:10px 12px` 补位，卡片外框高度维持 ~106px 不掉），
  配套放大字号与血条（姓名 1rem、血条高 10px）；侧栏在场人物头像 72 → **88px**。
- **自动存档移入设置**：侧栏底部那块「自动存档」小面板（−/输入框/＋ + 说明）删除，
  改为「设置」弹窗内第 4 个分组「回合自动存档」，沿用场景配额同款的 −/滑杆/＋ 三件套（0~10，0 = 关闭）。
  **「读取人生」一并移入该分组**（整宽按钮，排在说明下方），功能面板里不再单列；
  弹窗本体仍挂在侧栏，设置弹窗只发 `openSaves` 信号。

### 推进选项协议 v2（治「生硬」与「执着旧元素」）
- 元信息由五字段减为四字段：`（类型 | 距离 | 主动方 | 推进轴）`——删掉基本随机的「强度」，
  新增「距离」分档：**贴身 / 邻近 / 旁支 / 远离**，4 条须至少覆盖 3 档；
  「贴身」（直接回应正文最后一刻）至多 1 条，正文末尾的悬念也至多 1 条选项指向——
  从结构上封死「四条集体扑向同一个钩子」。
- **旧元素搁置（防复读）**：3 回合前出现过、玩家从未选择推进的悬念元素（奇物、未拆传讯、
  旧人物等）一律视为已搁置，不得再进选项或正文主线；解禁需玩家点名 / 直接阻碍当前目标 /
  间隔足够久后作为「返场」重新引入。剧情 AI 规则同步加第 18 条（含「禁止复读式提醒」——
  不许旁白周期性重述「那块奇怪的石头」来制造悬念）。
- **上轮选项回传**：程序把上一轮 4 条选项正文注入状态 AI，本轮至多 1 条与上轮核心对象相同
  ——玩家没选的就是拒绝了，不重复端上来。
- **反模板化写作**：删掉「五要素清单」（触发事件/参与者/动作/冲突/结束状态——填空式提纲的元凶），
  改为「从一个具体瞬间切入的进行时场景」（对白、动作或感官细节起笔）；长度 1~4 句错落；
  至少 1 条以对白开场、至少 1 条由他人/环境主动发起、四条起笔动词不得同类。
- 取材边界松绑：「当前现场」≠「正文最后一句」，当前地点、在场人物、身上物品、进行中的局势都是素材。
- 解析器兼容新四字段、旧五字段（承接/强度降级为兼容字段）与旧四倾向对象；开局版预设同步。

### 势力系统（可视化）
- 新增势力档案：宗门 / 世家 / 商会 / 魔道等，记录驻地、战力（元婴·结丹·筑基人数）、诉求、与主角关系。
  **强度档位由程序按战力数字推导**（元婴坐镇 / 一方势力 / 中等 / 小 / 微末），不让 AI 自评以免口径漂移。
- 两条写入来源：①侧边栏「势力」面板里手动点「探查」，由独立 AI 链路生成 3~6 家并直写；
  ②状态 AI 每回合用 `<MJ_FACTION_TAG>` 增量上报（只在首次出现 / 诉求关系变化 / 真实战力曝光时写，
  绝大多数回合为空）。未登记的势力不允许凭空更新。
- 势力快照以「【势力素材…】」注入剧情 AI，让事件从既有势力的诉求与关系里长出来，而不是每回合现编新势力。

### 篇章系统
- 「剧情脉络」升级为**主线**（整体长期方向）；其下新增**篇章**——一个具体的短期目标，
  只作用于剧情 AI 与状态 AI。**不开启就完全不注入**，AI 看不到任何篇章字样。
- 主线措辞重写：删掉旧版「冲突时以剧情自洽为先」（等于授权 AI 不理它），改为
  **可以推后、不可以放弃**，并要求连续数回合内有朝它靠近的可见累积；主线同时新增注入状态 AI，
  让世界与人物的长期演化也跟着走，而不是只影响叙事文案。
- 开启后每回合要求以「推进 / 受阻 / 铺垫 / 背景压力」四选一服务它；玩家在做别的事时篇章退为背景，
  绝不硬拽回主线；收束不等于成功（失败、放弃、搁置都算）。
- 超过回合上限后逐级温和加压（催收拢 → 催出结果 → 本回合必须推向结果），**不设硬闸**。
  回合上限在「设置」里可调（5~40，默认 15），跨人生生效。
- 与世界设定不同：篇章改动即时生效，不进待应用队列；进度（已进行回合数）随存档保存。

### 无目标场景的停留（治「闲聊两回合就道别」）
- 叙事质量预设新增 `[无目标场景的停留]`：游玩 / 闲谈 / 宴饮 / 赶路 / 市集闲逛 / 洞府日常这类
  没有确切目标的场景**不以"结束"为写作目标**；禁止"回合末道别"式收束（告辞、各自散去、一夜无话）。
- 离场只允许三种情况：玩家明确要走、情境自然耗尽（天黑 / 打烊 / 对方有事）、出现必须换地方才能办的事。
- 停留时该写什么给了正向清单：把对话往深处推一句、一件正在发生的小事、一个被记住的细节、
  关系的一次微小变化、或一个自然长出来的新钩子；每回合至少留一处新信息/新态度/新疑问。
- 落在**固定预设**（`narrative_preset.ts`）而非玩家可编辑的「规则」默认值，老存档也立即生效。

### 画像锁定改为只认勾选框
- 旧行为：改动性格 / 外貌 / 记忆任意一段文字，保存时就被判定为 `source: "manual"`，
  等于「一改档案 AI 就永久停更」。现在 `wantAi` 只看玩家手里那个勾选框，改文字不再顺带锁定。
- 队列分支同步修正：只改勾选框（内容未变）时也会重写待应用草稿，否则这类保存会被丢掉。

### NPC 记忆改为三行日志（只追加）
- 格式硬约束：一条 = 三行（时间行 / 地点行 / 正文行），条目之间空一行，**新的在最上面**；
  时间取本轮推进后的世界时间，分钟位恒写 `00`（世界时间模型只到小时，禁止编造分钟）；
  跨日连续事件写区间（`0001年05月17日-0001年05月18日`）。
- **只追加不改**：除非超字数触发压缩，否则不得更改已记录的事；更正靠新增条目，不回头改旧条目。
- **压缩 = 提炼，不是删除**（老板澄清）：超 1000 字时把旧条目压短到约 700 字（保留事实、删重复与废话，
  正文压到一句；同一时段相邻条目可合并成一条，时间行写区间），**不得整条丢掉旧条目凑字数**。
- 程序兜底（新模块 `src/role_core/memoryLog.ts`）：`guardMemoryUpdate()` 按时间行逐条比对——
  **旧记忆未超阈值时**严格只追加，发现改旧条目即整段拒绝并回滚为「旧条目原样 + 新条目追加到顶部」+ 告警；
  **旧记忆已超阈值（压缩轮）**放行提炼，否则压缩根本做不了。
  `Character.applyAiProfile` 是唯一写入通道。旧存档里没有时间行的散文式记忆一律放行不做校验。
- 状态 AI 输入新增精确时间（`formatWorldTimeZhPrecise`，带「时」），否则记忆的时间行无从落笔。

## 2026-09-21

### 角色设定可改「简介」+ 主角记忆系统（AI 维护）
- **简介可编辑**：`NpcBasicsDraft` 新增 `identity` 字段，角色设定 → 基本信息里新增「简介」输入框
  （角色卡与信息界面里名字下面那行「身份 · 境界」的可编辑部分）；队列、存档序列化、dirty 判定同步。
- **主角也有记忆日志了**（AI 维护）：
  - 新标签 `<MJ_PROTAGONIST_MEMORY_TAG>`（输出契约第 16 段）。两种形态：常规 `{"entries":[...]}`
    逐条追加；超 1000 字时 `{"full":"提炼后的整段","entries":[...]}` 走压缩（提炼，不删条目）。
  - 格式与 NPC 记忆完全一致（时间行 / 地点行 / 正文行，新的在最上面），主角视角。
  - 写入通道：`Character.appendAiMemoryEntries()`——**只动 memory**，性格与外貌不由 AI 管；
    主角画像整体锁为玩家设定时也不影响记忆维护（走的是独立通道，不经 `applyAiProfile`）。
  - 同样受 `guardMemoryUpdate` 保护：未超阈值时只追加，改旧条目会被回滚。
  -   状态 AI 输入新增【主角记忆】段（含字数与压缩提示）；角色设定里提示语改为
    「性格与外貌由玩家设定，AI 不会改写；记忆由 AI 每回合维护」。

### 修「不生成推进选项 / 有战斗却不触发」
排查出三处成因，一并修掉：
- **① 示例教坏了 JSON**：记忆日志示例在 JSON 字符串里写了**裸换行**，模型照抄后整段
  `<NPC_NEARBY_TAG>` 解析失败 → 人物卡全丢、战斗因「参战者缺卡」被拦。
  示例改回 `\n` 转义；解析层新增 `escapeRawNewlinesInStrings()`（只在字符串字面量内转义），
  `tryParseJsonArray` 多一层容错，多行记忆不再打废整段 JSON。
- **② 编号撞车**：新加的「数量词落成条目」占了 1.4~1.6，与原 1.4~1.6（无名群众 / 新增上限 / 既有 NPC）
  重号且内容冲突。已重排为 1.7~1.9。
- **③ 输出膨胀 / 提前收笔**：记忆日志每轮整段回吐会让输出变长，模型易在末尾两段标签前收笔。
  新增规则：没有新增记忆时**省略 memory 字段**（程序沿用旧值，不再整段抄）；
  输出契约新增「收尾两条不得省略」——第 11 段（触发）与第 13 段（4 条选项）必须完整输出后才算结束。
- **兜底重试**：本回合既无选项也无战斗触发时，带一句点名要求重试一次，且**只取选项与战斗触发**，
  状态不重复应用（避免修为/物品/时间被算两遍）；仍失败则记 warn。

### 取消「功法已不入流则修炼不产修为」
- 移除整套修为门槛：练哪一阶功法都照常涨修为，功法阶层**只**影响跨阶压制系数（打起来吃亏），
  不再卡修为，也不再要求 AI 输出 `xiuweiIncrease: 0` 或写"灵气不肯入体"这类描写。
- 删除 `itemTier.ts` 的 `isGongfaObsolete` / `describeGongfaCultivation`；
  `Protagonist.applyStateChanges` 的修为折算回到「xiuweiIncrease + 全部练习经验」直算；
  状态 AI 的功法摘要不再带【已不入流】标记；天道编辑与功法详情的阶层提示同步去掉该文案。
- 保留：跨阶压制系数（`gongfaTierFactor`）、老存档补阶层迁移、品阶倍率——这些与门槛无关。

### 修「剧情五个人只建一张卡」
- 根因一（程序）：`npcStore.applyNpcUpdates` 按 displayName 索引，AI 输出「五名守山弟子」
  这类**同名条目**时，5 条会全部命中同一个人 → 塌成 1 张卡。
  现改为同回合内第二次出现的同名条目视为**另一个人**，自动改名（守山弟子（2）…）单独建卡。
- 根因二（提示词）：`state_preset` 数量对齐新增 1.4/1.5/1.6——确数必须落成条目
  （"五名守山弟子"= 5 条）、**禁止同名条目**、把「正文五个人却只建一条」列为反模式；
  `story_preset` 新增 17.7：写确数就要给得起可辨识的区分（甲/乙/丙 或 为首那个…），
  写不起就写"几名/数名"。

### 事件来源分类学
- 剧情规则新增第 19 条：每回合事件须可归入 人物类 / 势力类 / 自身类 / 天地类；
  天地类（天降机缘、忽然顿悟）必须先有铺垫且连续不超过 2 回合，并列出反模式清单。

### 修「我在记忆里写的东西会被 AI 改」
根因在程序兜底 `guardMemoryUpdate` 的三处漏洞（提示词早就写了「只追加不改」，但拦不住）：
- **① 旧记忆不是三行日志格式就整段放行**：玩家手写的常常是**没有时间行的自由散文**，
  旧逻辑一看到整段不是日志格式就原样采信 AI 的输出 → 玩家写的内容一轮就被重写掉。
  现在改成**逐条保护**：无时间行的条目靠文本相似度（Dice 二元组）认亲，
  认出是旧内容的润色版就丢 AI 的版本、保玩家原文。相似度兜底只在「两边都无时间行」时启用
  （否则同一地点写两条日志，光地点行就够让相似虚高，会把新条目误吞）。
- **② 删条目检测不到**：旧逻辑只比对「AI 交回来的条目」，AI 少抄一条 = 那条直接消失，
  不判违规 → 有些模型只回新条目时，整段旧日志会被冲光。现在「丢失」单独计数并触发回滚。
- **③ 主角记忆整段（full）越权**：契约是未超阈值时只给 `entries`，但模型常顺手回整段。
  现在 `appendAiMemoryEntries` **只在压缩轮（>1000 字）接受 `full`**，其余一律只取新增条目。
- 顺带：比对用「逐行 trim 后的指纹」，AI 只多敲个空格不再被误判成新条目（避免旧条目被重复堆一份）；
  回滚日志改为同时报「改写 N 条 / 丢失 M 条」。
- 提示词补硬规则：`state_preset` 画像规则新增 3.5、主角记忆规则新增 8——
  **无时间行的自由散文 / 玩家手写条目一律原样保留**，不得改写、删除或改成三行格式。
- UI：角色设定里「性格与外貌由老板设定」改为「由**玩家**设定」（游戏并非单人自用，
  不再用「老板」称呼）；记忆字数提示改为常驻，明确「未超 1000 字时 AI 只追加，你写的内容不会被改写」。
- 实测（esbuild + node 跑守卫函数）：漏抄旧条目 / 润色旧条目 / 玩家散文被改 / 正常追加 / 压缩轮
  五种场景全部符合预期，且正常追加不会被误判。
